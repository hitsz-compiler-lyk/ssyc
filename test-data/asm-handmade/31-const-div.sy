int main() {
    int t = getint();
    while(t){
        t = t - 1;
        int x = getint();
        putint(x / -1);
        putch(10);
        putint(x / -2);
        putch(10);
        putint(x / -16);
        putch(10);
        putint(x / -14311323);
        putch(10);
        putint(x / -645334);
        putch(10);
        putint(x / -8913);
        putch(10);
        putint(x / 1);
        putch(10);
        putint(x / 42123);
        putch(10);
        putint(x / 2);
        putch(10);
        putint(x / 8);
        putch(10);
        putint(x / 65536);
        putch(10);
        putint(x / 412231);
        putch(10);
        putint(x / 341);
        putch(10);
        putint(x / 6412637);
        putch(10);
    }
    return 0;
}